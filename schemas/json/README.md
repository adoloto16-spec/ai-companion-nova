# Schema export boundary

Canonical cross-language schemas live under `contracts/schemas/`.

This directory is reserved for future generated TypeScript/Rust schema exports so there remains one source of truth.
