# Security tests

This suite targets caller-controlled privilege escalation, actual-target scope validation, capability enforcement, parameter schema validation, confirmation policy and audited failures.