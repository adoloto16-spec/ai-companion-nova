{
  "name": "ai-companion-nova",
  "private": true,
  "version": "0.2.0",
  "scripts": {
    "generate:schemas": "node scripts/generate-schema-catalog.mjs",
    "typecheck": "tsc -p tsconfig.foundation.json --noEmit",
    "build": "pnpm generate:schemas && tsc -p tsconfig.foundation.json",
    "test:architecture": "node scripts/check-architecture.mjs",
    "test:security": "pnpm build && node build/tests/security/action-broker.security.test.js",
    "test": "pnpm build && node build/tests/foundation.test.js && node build/tests/contracts/chat-contract.test.js && node build/tests/unit/ai-runtime.test.js && node build/tests/unit/openai-compatible-chat-provider.test.js && node build/tests/integration/ai-runtime.integration.test.js && node build/tests/security/action-broker.security.test.js && node scripts/check-architecture.mjs",
    "desktop:dev": "pnpm exec tauri dev --config apps/desktop-host/src-tauri/tauri.conf.json"
  },
  "workspaces": [
    "apps/*",
    "contracts",
    "sdk/typescript",
    "core",
    "modules/mock",
    "providers/mock",
    "host/ipc",
    "host/credentials",
    "runtime/bootstrap"
  ],
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "typescript": "^5.8.3",
    "@types/node": "^24.0.0",
    "@tauri-apps/cli": "2.11.5"
  }
}
